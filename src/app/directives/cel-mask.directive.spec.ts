import { CelMaskDirective } from './cel-mask.directive';

describe('CelMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CelMaskDirective();
    expect(directive).toBeTruthy();
  });
});
